# ha-inteno-router

Home Assistant custom integration for the Inteno/IOPSYS home router —
stats, connected devices, and reboot control via the router's `ubus` API.

See `AGENTS.md` for architecture and setup.
